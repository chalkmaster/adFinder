import template from './home.html';
import controller from './home.controller';

let homeMain = {
    url: '/home',
    template,
    controller,
    controllerAs:'$ctrl'
};

export default homeMain;
